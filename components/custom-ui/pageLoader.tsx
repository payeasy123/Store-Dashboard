import { getAsset } from "@/utils";
import Image from "next/image";

export const PageLoader = () => {
    return <div className="z-[8] flex h-screen w-screen flex-col items-center justify-center bg-transparent">Page Loading</div>;
};
